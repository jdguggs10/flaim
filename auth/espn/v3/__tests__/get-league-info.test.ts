import { getLeagueInfo, getLeagueInfoSafe } from '../get-league-info';

describe('getLeagueInfo', () => {
  // Skip actual API calls in tests to avoid hitting ESPN's API
  it('should be tested with integration tests', () => {
    expect(true).toBe(true);
  });
});
