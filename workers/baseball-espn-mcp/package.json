{
  "name": "baseball-espn-mcp",
  "version": "4.0.0",
  "description": "Open access MCP server for fantasy sports integration",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dev": "wrangler deploy --env dev",
    "deploy:prod": "wrangler deploy --env prod",
    "test": "jest",
    "test:watch": "jest --watch",
    "type-check": "tsc --noEmit",
    "tail": "wrangler tail",
    "tail:dev": "wrangler tail --env dev"
  },
  "dependencies": {
    "@clerk/backend": "^1.34.0",
    "@cloudflare/agents": "0.0.16",
    "@cloudflare/workers-types": "^4.0.0",
    "@flaim/auth": "file:../../auth"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.25.0"
  },
  "engines": {
    "node": ">=22"
  }
}
