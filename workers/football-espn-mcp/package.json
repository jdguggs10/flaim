{
  "name": "football-espn-mcp",
  "version": "1.0.0",
  "description": "ESPN Fantasy Football MCP server with shared authentication",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dev": "wrangler deploy --env dev",
    "deploy:prod": "wrangler deploy --env prod",
    "test": "jest",
    "test:watch": "jest --watch",
    "type-check": "tsc --noEmit",
    "tail": "wrangler tail",
    "tail:dev": "wrangler tail --env dev"
  },
  "dependencies": {
    "@clerk/backend": "^1.34.0",
    "agents": "^0.2.30",
    "@cloudflare/workers-types": "^4.0.0",
    "@supabase/supabase-js": "^2.52.0"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^24.0.3",
    "jest": "^29.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.25.0"
  },
  "engines": {
    "node": "24.x"
  }
}
